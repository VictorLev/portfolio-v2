docker_commands

.terraform
.terraform.d
.terraform.lock.hcl
*.tfstate
*.tfstate.backup

# Ignore all .tfvars files, except for exactly one
#
local.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json



.env
